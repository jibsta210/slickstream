<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Splash screen theme (androidx.core:core-splashscreen). -->
    <style name="Theme.SlickStream.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.SlickStream</item>
    </style>

    <!-- Base app theme. Compose drives the real colors; this just paints system chrome. -->
    <style name="Theme.SlickStream" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="o_mr1">shortEdges</item>
    </style>
</resources>
